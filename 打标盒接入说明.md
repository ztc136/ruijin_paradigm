# ESP32-S3 打标盒接入说明

## 结论

旧系统中眼动仪只负责向 ESP32-S3 发送 marker 串口帧。ESP32-S3 再把 marker 转成 8 位并行数据和 strobe/enable 脉冲送入采集设备。因此眼动仪缺失后，可由本 PsychoPy 范式直接向同一打标盒发送完全相同的串口帧。

## 已确认的旧固件协议

- 实物开发板型号：ESP32-S3-N16R8。
- 串口：115200 baud、8 data bits、no parity、1 stop bit。
- 每个 marker 帧：`0x36` 帧头 + 1 字节 marker。
- marker 范围：0–255；本范式使用 1–255。
- marker bit0–bit7：ESP32 GPIO2–GPIO9。
- strobe/enable：ESP32 GPIO10。
- GPIO10 空闲为高；数据就绪后拉低 1 ms，再拉高。采集设备应在这个上升沿锁存数据。
- 上升沿后数据继续保持 1 ms，然后 GPIO2–GPIO9 清零。

实物飞线已确认按编号 1–8 引出：编号线1–8分别对应 GPIO2–GPIO9，也就是 marker 的 bit0–bit7。开发板丝印中的 `CLK/CMD/D0–D3` 不是本打标盒的 marker 数据线，不按该丝印接采集端。

| 编号线 | GPIO | marker 位 | 权值 |
|---:|---:|---:|---:|
| 1 | 2 | bit0 | 1 |
| 2 | 3 | bit1 | 2 |
| 3 | 4 | bit2 | 4 |
| 4 | 5 | bit3 | 8 |
| 5 | 6 | bit4 | 16 |
| 6 | 7 | bit5 | 32 |
| 7 | 8 | bit6 | 64 |
| 8 | 9 | bit7 | 128 |
| trigger | 10 | strobe | — |

## 推荐链路

```text
PsychoPy win.flip()
  └─ COM串口发送 [0x36, marker]
       └─ ESP32-S3
            ├─ GPIO2–GPIO9 → Blackrock D0–D7
            └─ GPIO10      → Blackrock DS/strobe

同一次翻屏还可发送同值 LSL marker，用于独立日志和交叉核对。
```

程序启动时选择：

- `serial`：只通过 ESP32 打标盒向采集设备打标。
- `serial+lsl`：推荐；ESP32 硬件打标和 LSL 同时发送。

“端口或地址”填写 Windows 识别到的 COM 口，例如 `COM3`。

## 连接与验收

1. 连接打标盒 USB，双击 `检测打标盒串口.bat`，确认出现 COM 口及设备描述。尚未连接采集设备时，先按 `示波器测试说明.md` 完成台架测试。
2. 在 Blackrock Central 中开始一次测试记录，并确认数字输入事件已启用。
3. 在命令行运行：

   ```powershell
   & "C:\Program Files\PsychoPy\python.exe" trigger_box_test.py --port COM3
   ```

4. 采集端应依次出现：`1, 2, 4, 8, 16, 32, 64, 128, 85, 170, 99`。
5. 逐位序列可检查 D0–D7 是否有反接、缺失或位序颠倒；`85/0x55` 与 `170/0xAA` 可检查交替位。
6. 再运行范式测试模式，核对完整事件序列，例如：

   ```text
   1 → 10 → 21/22/23 → 30 → 41–49 → 50(视频范式) → 60 → … → 99
   ```

7. 同时使用 LSL 时，在另一台电脑运行 `lsl_marker_monitor.py`，核对 LSL 与 Blackrock 中的 marker 值和顺序一致。

## 安全与时序注意

- 必须连接公共地，并核对现有打标盒是否已经包含电平转换、隔离和 Blackrock 接口板。
- 不要在未核对电平与针脚的情况下直接把裸 ESP32 GPIO 接入临床采集设备。
- ESP32 GPIO 为 3.3 V 逻辑；需由设备负责人确认现有接口盒对 Blackrock TTL 输入兼容。
- 串口两字节在 115200 baud 下传输约需 0.17 ms，固件再等待约 1 ms 才产生 strobe 上升沿。最终实际延迟和抖动应使用示波器、光电二极管或采集端事件时间实测。
- LSL 用于交叉校验，Blackrock 数据中的硬件数字事件应作为离线分析的主 marker。
