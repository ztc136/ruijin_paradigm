# 打标盒示波器测试说明

本测试只验证“电脑串口 → ESP32 打标盒 → 8 位数据线和 strobe”链路。未确认接口针脚、地线和电平前，不连接患者、医院采集设备或医院机房地线。

实物已确认为 ESP32-S3-N16R8：GPIO2–GPIO9依次连接编号线1–8（marker bit0–bit7），GPIO10为 trigger/strobe。开发板自身丝印的 `CLK/CMD/D0–D3` 不属于 marker 总线。

## 1. 预备与安全

- 普通台式示波器探头的地夹通常与保护地相连。只把地夹接到已确认的打标盒逻辑 GND，不能夹到数据线、strobe、电源正极或未知针脚。
- 不确定打标盒是否隔离、接口定义是否正确时，优先使用差分探头或隔离/电池示波器，并由医院设备工程师复核。
- 先断电，用万用表通断档和原理图/丝印确认 GND、D0–D7、DS/strobe；使用抓钩探头，避免探针滑动造成相邻针脚短路。
- 示波器探头设为 ×10、DC 耦合；打标盒只通过 USB 数据线连接测试电脑。

## 2. 电脑识别

双击 `检测打标盒串口.bat`。若没有 COM 口：检查打标盒供电、换一根确认可传数据的 USB 线、换 USB 口，并在设备管理器中查看是否缺少 USB-UART 驱动。

识别到例如 `COM3` 后，以下命令均在 `emotion_paradigm` 目录运行：

```powershell
& "C:\Program Files\PsychoPy\python.exe" trigger_box_test.py --port COM3 --code 1 --count 20 --interval 1
```

## 3. 示波器设置

- CH1：GPIO10 / trigger / DS / strobe；触发选 CH1 上升沿，阈值约 1.5 V。
- CH2：正在检查的编号数据线，例如编号线1 / GPIO2 / bit0。
- 两个探头地夹接同一个已确认的逻辑 GND。
- 垂直档先用 1 V/div 或 2 V/div；时基先用 500 µs/div，触发稳定后再放大。

预期波形：逻辑高约 3.3 V、低约 0 V；strobe 空闲为高。接收 marker 后，数据位先就绪，strobe 拉低约 1 ms，再出现上升沿；上升沿后数据继续保持约 1 ms，然后全部数据位清零。采集设备应在 strobe 上升沿读取 D0–D7。

## 4. 逐位和组合编码检查

依次运行下列编码，并把 CH2 换到相应数据位：

| marker | 应为高电平的编号线 | ESP32 引脚 |
|---:|---|---|
| 1 | 1 | GPIO2 |
| 2 | 2 | GPIO3 |
| 4 | 3 | GPIO4 |
| 8 | 4 | GPIO5 |
| 16 | 5 | GPIO6 |
| 32 | 6 | GPIO7 |
| 64 | 7 | GPIO8 |
| 128 | 8 | GPIO9 |
| 85 / 0x55 | 1、3、5、7 | GPIO2、4、6、8 |
| 170 / 0xAA | 2、4、6、8 | GPIO3、5、7、9 |

例如：

```powershell
& "C:\Program Files\PsychoPy\python.exe" trigger_box_test.py --port COM3 --code 0x55 --count 20 --interval 1
```

也可发送一次完整诊断序列：

```powershell
& "C:\Program Files\PsychoPy\python.exe" trigger_box_test.py --port COM3 --interval 1
```

完整序列应为 `1, 2, 4, 8, 16, 32, 64, 128, 85, 170, 99`。

## 5. 建议记录的验收结果

- 每条线的低/高电压，以及 D0–D7 位序是否正确。
- strobe 低电平宽度、数据在上升沿前后的保持时间。
- 固定编码连续发送 1000 次，示波器脉冲计数是否为 1000，是否有毛刺或漏脉冲。
- 若有光电二极管：CH1 接 strobe，CH2 接屏幕光电信号，在真实 PsychoPy 范式中测量视觉翻屏到硬件 marker 的延迟分布。只测 strobe 本身不能代表真实刺激呈现时刻。

发现高电平不是约 3.3 V、strobe 方向/宽度不符、数据位在 strobe 上升沿不稳定或有过冲毛刺时，先停止接入采集设备并保存示波器截图。
